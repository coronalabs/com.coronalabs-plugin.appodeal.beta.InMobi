Sources for the plugin `plugin.appodeal.beta.InMobi`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.InMobi" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
